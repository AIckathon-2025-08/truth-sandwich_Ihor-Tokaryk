<!DOCTYPE html>
<html>
<head>
  <title>Edit <%= @user.full_name %> - Truth Sandwich</title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>
  <div class="container">
    <h1>✏️ Edit <%= @user.full_name %></h1>
    
    <div style="margin-bottom: 20px;">
      <a href="/users/<%= @user.id %>" class="btn btn-secondary">Back to User</a>
      <a href="/users" class="btn btn-secondary">All Users</a>
    </div>
    
    <% if @user.errors.any? %>
      <div class="errors">
        <h3>Please fix the following errors:</h3>
        <ul>
          <% @user.errors.full_messages.each do |error| %>
            <li><%= error %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    
    <form method="post" action="/users/<%= @user.id %>" enctype="multipart/form-data">
      <input type="hidden" name="_method" value="put">
      
      <div class="form-group">
        <label for="first_name">First Name:</label>
        <input type="text" id="first_name" name="first_name" value="<%= @user.first_name %>" required>
      </div>
      
      <div class="form-group">
        <label for="last_name">Last Name:</label>
        <input type="text" id="last_name" name="last_name" value="<%= @user.last_name %>" required>
      </div>
      
      <div class="form-group">
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="<%= @user.email %>" required>
      </div>
      
      <div class="form-group">
        <label for="position">Position:</label>
        <input type="text" id="position" name="position" value="<%= @user.position %>" required>
      </div>
      
      <div class="form-group">
        <label for="image">Profile Image:</label>
        <% if @user.has_image? %>
          <div style="margin-bottom: 10px;">
            <img src="<%= @user.image_url %>" alt="Current profile image" class="image-upload-preview">
            <form method="post" action="/users/<%= @user.id %>/image" style="display: inline; margin-left: 10px;" onsubmit="return confirm('Are you sure you want to delete this image?')">
              <input type="hidden" name="_method" value="delete">
              <button type="submit" class="btn btn-danger btn-small">Delete Image</button>
            </form>
          </div>
        <% end %>
        <input type="file" id="image" name="image" accept="image/*">
        <small>Supported formats: JPG, PNG, GIF, BMP</small>
      </div>
      
      <div class="form-group">
        <label>
          <input type="checkbox" name="admin" <%= @user.admin? ? 'checked' : '' %>>
          Admin User
        </label>
      </div>
      
      <div class="form-actions">
        <button type="submit" class="btn">Update User</button>
        <a href="/users/<%= @user.id %>" class="btn btn-secondary">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
