<!DOCTYPE html>
<html>
<head>
  <title>Users - Truth Sandwich</title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>
  <div class="container">
    <h1>👥 Users</h1>
    
    <div style="margin-bottom: 20px;">
      <a href="/users/new" class="btn">Add New User</a>
      <a href="/" class="btn btn-secondary">Home</a>
    </div>
    
    <% if @users.empty? %>
      <p>No users found. <a href="/users/new">Create the first user</a>.</p>
    <% else %>
      <table class="users-table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Position</th>
            <th>Admin</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= user.full_name %></td>
              <td><%= user.email %></td>
              <td><%= user.position %></td>
              <td><%= user.admin? ? '✅' : '❌' %></td>
              <td class="actions">
                <a href="/users/<%= user.id %>" class="btn btn-small">View</a>
                <a href="/users/<%= user.id %>/edit" class="btn btn-small btn-secondary">Edit</a>
                <form style="display: inline;" method="post" action="/users/<%= user.id %>" onsubmit="return confirm('Are you sure you want to delete this user?')">
                  <input type="hidden" name="_method" value="delete">
                  <button type="submit" class="btn btn-small btn-danger">Delete</button>
                </form>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
  </div>
</body>
</html>
